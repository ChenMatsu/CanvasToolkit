import { useTranslation } from "react-i18next";
import { BiLayout, BiText, BiPhotoAlbum, BiCategory, BiFullscreen } from "react-icons/bi";
import "./Sources.scss";

const Sources = () => {
    const { t } = useTranslation(["Layout"]);

    return <div id="sources">123</div>;
};

export default Sources;
